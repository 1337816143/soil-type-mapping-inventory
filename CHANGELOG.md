# Changelog

## v1.0.15 — 2026-08-12

- 修复 Android/部分移动浏览器点击管理员文件选择后只进入“照片/视频”选择界面、无法直接选择 ZIP 的问题。
- 新增“选择 ZIP 文件（手机推荐）”专用入口：使用 `.zip`、`application/zip`、`application/x-zip-compressed` 非媒体类型调用系统文件选择器，避免 Chromium/Android 将请求交给照片选择器。
- 原“选择文件”入口继续保留所有文件类型能力，同时显式加入通用文件与 ZIP 类型并移除 `capture`，不删除既有参考资料、PPTX、DOCX、PDF 等导入能力。
- 手机专用 ZIP 入口直接复用既有 JSZip 解压、管理员导入状态、北部28份权威索引和自动识别流程；解析完成后自动生成逐文件预览，不需要额外手工录入任务单元。
- 新增移动端 ZIP 文件选择回归测试，并将该模块纳入版本升级、CI 和 Pages 部署校验。
- 本次仅修复前端文件选择入口；内置 GitHub Token 字节内容、Git Data API 上传链路、北部28份权威关联、整改答复、删除和统计逻辑均保持不变。

## v1.0.14 — 2026-08-12

- 将河北省北部片区28份质控报告从“预关联”升级为权威确认索引：依据实际收到的ZIP逐文件核对文件名、大小和SHA-256，并为6类成果保存明确的市、作业单位、任务单元关联。
- 修正共享报告模型：一份报告对应多个任务单元属于正常关系，不再要求共享文件必须收敛到唯一市/单位/任务单元；预览中直接显示来源地区数、实际任务单元数和成果类型数。
- 修复雄安新区特殊口径：土壤类型图以“雄安新区”作为汇总任务单元，雄县、安新县、容城县、雄安新区本级在该成果下折叠到同一汇总任务；其他5类成果继续保持雄县、安新县、容城县等独立任务。
- 权威索引同步进入页面统计。尚未真实归档到仓库的原文件使用“已登记·待归档”状态，不生成可点击的失效文件链接；文件实际归档后自动恢复正常查看链接。
- 当前28份材料继续作为自动识别基准：命中登记文件后自动带出第一轮、6类成果和全部共享关联，不再显示“任务单元不唯一”的错误提示。
- 保留原有管理员导入、39 MiB整文件/分块上传、GitHub Actions归档、整改答复、删除、统计与参考资料功能；内置GitHub Token字节内容未修改。

## v1.0.13 — 2026-08-12

- 修复ZIP解压完成后自动识别被基础导入器内部 setFiles() 绕过的问题：直接监听导入状态 state.files 的赋值，确保ZIP预览生成后立即重新识别。
- 当前北部28份共享质控报告按预登记文件名/规范化文件名和大小自动匹配，自动恢复第一轮、6类成果和全部共享任务单元。
- 扩展历史第一批、第二批、第二批补充、第三批等旧资料识别规则，兼容土壤类型图、土壤属性图、耕地质量评价/等级/耕评等旧命名格式及目录结构。
- 增强地区与任务单元反推，兼容信都县→信都区、南和县→南和区、沙河县→沙河市、石家市→石家庄市等历史名称。
- 自动识别增加有限次数重绑定，覆盖管理员弹窗延迟重建与拆分文件后的状态更新；仍不使用长期 MutationObserver。
- 新增当前北部登记材料与历史文件名回归测试，并继续保护既有上传、答复、删除、统计与Pages部署链路。

## v1.0.12 — 2026-08-12

- 将本次河北省北部片区28份质控报告正式标记为“预关联”材料库：每份文件预先登记文件名、大小、SHA-256、第一轮批次、关联任务单元和6类成果；实际上传时按登记信息自动校验，不需要重新逐项配置。
- 管理员导入新增自动识别器：根据文件名、目录名和已登记材料自动判断“质控意见/参考资料”、成果类型、批次、市、作业单位和任务单元；完全识别时隐藏手工下拉框，仅保留“显示人工调整”作为兜底。
- 成果类型支持逐文件自动识别，同一次质控导入中不同文件可以属于不同成果类型；一份综合质控文件也可同时关联多个成果类型，索引按成果类型展开，但物理文件仍只保存一次。
- 北部原ZIP继续支持一次选择后自动解压；已登记的28份文件使用“文件名+大小”前置匹配，并在GitHub Actions归档阶段再使用SHA-256校验实际字节，避免误传同名错误文件。
- 北部上传适配器移除长期 `MutationObserver`，改为对现有渲染函数做一次性包装和受控刷新，避免预览文本更新形成观察器自循环。
- 内置 GitHub Token 的字节内容未修改；既有 `fetch + Bearer` Git Data API、39 MiB整文件/分块、整改答复、删除、统计和Pages部署链路保持不变。

## v1.0.11 — 2026-08-12

- 新增河北省北部片区共享质控报告规则：文件名中“第三次全国土壤普查成果质控报告”前列出的多个地区自动视为同一份报告的关联范围，仓库中只保存1份原文件，不再按地区或成果类型复制。
- 北部综合质控报告自动覆盖土壤类型图、土壤属性图、耕地质量等级评价、土壤退化与障碍分析、土特产品土壤适宜性评价、土壤农业利用适宜性评价6类成果；不自动关联土地资源评价与利用报告。
- 兼容北部现有命名差异和跨市组合：徐水县/满城/清苑/高阳/顺平等旧称自动映射到现任务单元；“雄安新区本级”“承德市本级”自动归一；“合并区”“全市”“市级”按前后已识别地区判断所属城市。
- 管理员上传共享质控报告时，上传预览显示“一份原文件→多个任务单元×6类成果”；混合暂存清单与 Actions 归档使用紧凑共享索引，使所有对应位置引用同一物理文件并同步更新收缴统计。
- 管理员可直接选择包含多份北部质控报告的 ZIP，系统先解压为报告文件，再依据每个文件名自动识别关联范围；当前材料批次加入“第一轮”。
- 登记本次上传包元数据：共28份 DOCX、总大小93,379,068字节，并记录文件名、大小、SHA-256、第一轮批次、关联地区和6类成果范围，用于导入校验与防重复。
- 新增北部共享质控路由回归测试，覆盖28个文件名、跨承德/张家口的“合并区/全市”上下文、用户示例9地区×6成果=54个页面关联，以及“一份报告始终仅引用一个物理路径”。
- 内置 GitHub Token 原值未修改；原始 `fetch + Bearer` Git Data API 上传链路、上传进度、3~5分钟成功提示、整改答复和既有功能均保持不变。

## v1.0.10 — 2026-07-30

- 修复管理员上传时报错 `Maximum call stack size exceeded`：原位进度脚本与成功提示脚本此前各自只在函数上记录“已包装”，两个 `MutationObserver` 会交替再次包装同一个 `SoilAdminImport.progress`，最终形成过深调用链。
- 两个进度增强改为在 `SoilAdminImport` 对象上记录一次性安装状态；观察器重复执行时函数引用保持不变，不再产生递归包装。
- 管理员上传状态监听改为有限次数等待界面就绪，不再使用全页面长期 `MutationObserver` 监听每一次文本更新。
- 恢复完整的 `upload-config.js` 原子启动、版本检查和模块加载逻辑，并将全部动态脚本缓存键统一更新为 v1.0.10。
- 新增动态回归测试：模拟两个观察器反复执行 200 轮并连续调用管理员进度 5000 次，验证函数不会再次包裹且不会发生调用栈溢出。
- 保留内置 Token 原值、原始 `fetch + Bearer` 上传链路、39 MiB 整文件/分块策略、管理员密码、Actions 归档、3~5 分钟成功提示及全部既有功能。

## v1.0.9 — 2026-07-29

- 修复管理员上传进度文本不断向下追加的问题：固定“当前阶段”和“当前百分比”两个字段，每次更新直接替换原位置内容，不再把上一条状态递归拼接到下一条状态。
- 撤销 v1.0.8 对全局 `window.fetch` 和 Git Blob 请求的 XHR 接管，管理员参考资料上传恢复为替换三普 Logo 之前已使用的原始 `fetch + Bearer + Git Data API` 链路。
- 保留代码中的内置 Token 原值，不删除、不置空、不改写；管理员导入继续跳过后来新增的重复凭证弹窗，直接使用既有上传链路。
- 上传阶段显示原位变化的百分比和进度条；由于浏览器原生 `fetch` 不提供请求体字节事件，明确采用有上限的阶段进度，不再伪装成逐字节实时日志。
- 新增内置 Token 在线 Git Data API 回归测试，分别验证读取 `main` 引用和创建微型 Git Blob；测试过程不输出 Token。
- 保留管理员密码、39 MiB 整文件/分块策略、GitHub Actions 归档、3~5 分钟成功提示及全部既有功能。

## v1.0.8 — 2026-07-29

- 修复管理员上传参考文件时进度长期停留在“正在整文件上传”的问题：GitHub Blob 上传改由可监听的 `XMLHttpRequest.upload` 传输层接管，显示实时上传字节和百分比。
- 本地文件读取、Base64 编码期间增加阶段提示和有限递增进度；文件发送后继续显示“等待 GitHub 确认写入”，不再长期停在初始状态后突然失败或成功。
- 管理员点击上传后先验证当前凭证；浏览器保存的临时 Token 失效时自动回退到代码中的内置 Token，不删除、不置空内置 Token。
- 单次管理员上传开始后固定使用同一个已验证凭证，防止其他异步模块在上传途中改变全局 Token，造成前序请求成功、文件上传阶段却返回 `Bad credentials`。
- 当前凭证和内置凭证均返回 401 时，在读取大文件前立即停止并给出明确提示，不再等待文件编码和网络上传后才报错。
- 保留管理员密码、39 MiB 整文件/分块策略、GitHub Actions 归档、3~5 分钟成功提示及全部既有功能。

## v1.0.7 — 2026-07-28

- 修复整改答复上传进度长期停留在“准备上传”的问题：恢复本地读取、网络传输、GitHub确认和完成四个阶段的实时进度。
- 使用 `XMLHttpRequest.upload` 监听真实上传字节；浏览器无法提供可计算进度时启用缓慢递增的兜底进度，避免界面假死。
- 上传完成后先显示100%成功状态，再更新原位置为“查看 / 替换”，并重新同步仓库答复索引。
- 新增原子启动遮罩：旧版基础页面和各增强模块在后台完成渲染后一次性展示，消除页面逐层变形和旧版内容闪现。
- 所有增强脚本统一使用当前版本缓存键，并在启动时核对部署 `VERSION`，发现版本不一致时自动带缓存破坏参数重新加载。
- 新增整改答复实时进度和原子启动回归测试，防止以上问题再次出现。

## v1.0.6 — 2026-07-28

- 按项目所有者明确要求恢复并固定内置 GitHub 上传 Token；未经明确授权不得删除、置空或改为必须手动输入。
- 恢复上传成功提醒：“上传成功！稍等3~5分钟刷新网站即可查看新上传的文件。”
- 整改答复上传不再要求管理员密码，直接使用内置 Token 完成推送。
- 修复整改答复文件名与页面位置的规范化匹配；上传成功后及重新刷新网站后，原位置均显示“查看”“替换”两个可用按钮。
- 增加真实已上传答复文件的回归测试、版本检查和维护约束，防止相关功能再次被误删或改回。

## v1.0.5 — 2026-07-28

- 参考文件目录改为读取 GitHub Pages 部署时生成的同源静态仓库清单，普通访客不再调用未认证 GitHub Tree API，避免共享出口 IP 触发每小时限流。
- 整改答复文件索引同步改为复用静态或已认证仓库树，不再额外发送未认证的 `contents/replies` 目录请求。
- 管理员设置 Fine-grained PAT 后，仓库树和整改答复索引使用认证 API 保持实时；认证请求失败会自动回退到静态清单。
- Pages 部署阶段使用 `GITHUB_TOKEN` 一次性生成 `data/repository-tree.json`，文件上传或删除触发重新部署后会自动刷新目录清单。
- 建立固定版本迭代规则和版本升级脚本；从本版本起，每次代码或功能修改均同步更新版本号与更新日志。

## v1.0.2 — 2026-07-27

- 将第三次全国土壤普查Logo替换为用户提供的透明PNG版本，页首与页脚统一使用新图。
- 取消旧Logo的放大裁切规则，完整保留新Logo的圆形外圈，并刷新页面资源缓存。

### 2026-07-28 安全与可访问性补丁

- 移除公开前端代码中内置的 GitHub 上传凭证，管理员 Token 改为仅在当前浏览器会话中保存，并自动清理旧版长期存储。
- 为成果页签、动态按钮、弹窗、状态提示、外链和数据表补充键盘操作、ARIA 语义及安全的外链属性。
- 保留既有上传、管理员导入、删除中心、批次对应、统计和参考文件功能，不做功能删减。

## v1.0.1 — 2026-07-27

- 将“土特产品适宜性评价”统一更名为“土特产品土壤适宜性评价”，旧参考目录自动归入新分组。
- 增加管理员删除中心，可批量删除质控意见、整改答复和参考文件，并在一次提交中同步更新质控索引。

## v1.0.0 — 2026-07-27

- 建立项目语义化版本号机制，并在页面显示当前版本。
- 片区摘要增加区县进度，压缩统计块尺寸，保持整体占用面积基本不变。
- 作业单位统计改为按规范化单位名称去重，片区与全省汇总使用集合并集，避免重复累加。
- 放大页首第三次全国土壤普查标识，并通过显示容器裁切外部白边，保留图形内部白色。
- 参考文件标准分组全部默认收起。
- 增加部署前数据与界面关键规则校验。