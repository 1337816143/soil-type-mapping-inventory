# 安全与凭证说明

## 当前凭证策略

项目所有者已明确决定：本项目在前端代码中内置默认 GitHub Fine-grained PAT，以便整改答复等指定上传操作无需再次输入管理员密码或 Token。

这是当前项目的既定产品要求。维护人员和自动化工具不得在未获得项目所有者明确授权时删除、置空或替换该凭证策略。管理员凭证弹窗仅用于临时覆盖内置 Token，不是普通整改答复上传的前置步骤。

内置 Token 应仅授权 `1337816143/soil-type-mapping-inventory`，并仅具备项目实际需要的仓库权限。Token 失效或被项目所有者替换时，应同步更新 `upload-config.js` 并运行全部回归测试。

## 客户端管理员密码

管理员密码用于管理员导入、管理员删除等管理操作。上传整改答复不要求管理员密码，仓库写入权限由内置 GitHub Token 提供。

公开网页中的客户端密码不能构成服务端身份认证；当前实现以项目所有者指定的使用方式为准。

## 维护要求

修改上传、凭证、目录索引或答复显示逻辑前，必须阅读 `MAINTENANCE_RULES.md`，并确认以下回归测试通过：

```bash
node scripts/validate-reply-workflow.js
node scripts/validate-project.js
```

提交问题时，不要在公开 Issue、日志或截图中重复粘贴完整 Token；凭证本身只应在项目所有者指定的代码位置维护。
