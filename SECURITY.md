# 安全说明

## GitHub 上传凭证

本项目部署在公开的 GitHub Pages 上，任何写入仓库的前端源代码都可被访问。因此：

- 仓库中不得内置 GitHub PAT、密码、私钥或其他长期凭证。
- 页面上传功能只接受管理员临时输入的 Fine-grained PAT。
- PAT 仅保存在当前浏览器会话的 `sessionStorage` 中，关闭浏览器后自动清除。
- PAT 应只授权 `1337816143/soil-type-mapping-inventory`，并仅授予完成上传、删除所需的 Contents 读写权限。
- 不应使用账号级高权限 Classic PAT。

## 凭证泄露处置

发现 Token 曾进入公开代码、提交历史、日志或截图时，应立即：

1. 在 GitHub 设置中撤销该 Token。
2. 新建权限范围更小的 Fine-grained PAT。
3. 检查仓库提交、Actions 日志和近期写入记录。
4. 清除浏览器中旧的站点存储并重新登录页面。

仅从当前分支删除 Token 不能使历史提交中的 Token 失效，撤销和轮换是必要步骤。

## 客户端管理员密码

公开网页中的客户端密码只能作为误操作提示，不能构成真正的身份认证。仓库写权限最终由 GitHub Token 决定。长期方案应将管理员认证与写入操作迁移到受保护的服务端接口或具备身份验证的 Worker。

## 报告问题

提交安全问题时，请勿在公开 Issue 中粘贴 Token、密码、Cookie、完整请求头或包含个人信息的文件。
