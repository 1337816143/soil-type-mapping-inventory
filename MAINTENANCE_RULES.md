# 项目维护约束

以下行为是项目所有者明确指定的现有需求。除非项目所有者在新的指令中明确要求变更，否则后续修改不得删除、弱化或替换这些行为。

1. **默认 GitHub Token 内置在前端代码中。** 不得擅自删除、置空、改为必须手动输入，或用其他凭证策略替换。
2. **管理员参考资料和质控文件上传沿用原始 Git Data API `fetch + Bearer` 链路。** 未经真实 API 回归验证，不得通过全局替换 `window.fetch`、XHR 代理或其他传输层重写接管 Git Blob 请求。
3. **内置 Token 必须通过自动化在线验证。** 至少验证读取 `main` 引用和创建微型 Git Blob；验证过程不得输出 Token。浏览器中的临时覆盖不得删除或置空内置 Token。
4. **管理员上传进度必须在原位置更新。** 当前阶段和当前百分比直接替换原有内容，不得把上一条状态递归拼接为不断增长的日志；进度条必须同步变化。
5. **同一个管理员进度函数不得被观察器反复包装。** 进度增强与成功提示增强必须使用 `SoilAdminImport` 对象级一次性标记；任何动态观察器重复执行后，`progress` 函数引用应保持稳定，不得形成递归调用链或调用栈溢出。
6. **北部片区综合质控报告允许“一份文件对应多个地区、多个实际任务单元”。** 这是正常数据关系，不得再用“任务单元必须唯一”的规则判错；同一份物理文件只允许在仓库中保存一次，不得为了多个任务单元或多个成果类型复制文件。
7. **北部片区当前这28份综合质控报告只确认关联3类主要成果：**土壤类型图、土壤属性图、耕地质量等级评价。不得再把这批综合报告自动扩展到土壤退化与障碍分析、土特产品土壤适宜性评价、土壤农业利用适宜性评价或土地资源评价与利用报告；如果以后确实收到这些成果的独立质控文件，应按其明确文件名单独识别和入库。
8. **北部片区文件名兼容既有工作命名。** 必须识别“徐水县→徐水区、满城→满城区、清苑→清苑区、高阳→高阳县、顺平→顺平县、雄安新区本级→雄安新区、承德市本级→承德市”，并根据权威索引或上下文判断“合并区、全市、市级”的所属城市。
9. **雄安新区土壤类型图采用汇总任务口径。** 雄县、安新县、容城县、雄安新区本级在土壤类型图中关联到“雄安新区”汇总任务；土壤属性图和耕地质量等级评价仍按雄县、安新县、容城县等独立任务单元关联。
10. **北部片区共享报告上传必须自动化。** 管理员可选择单个/多个报告或包含报告的ZIP；系统应按已确认索引和文件名自动解析关联范围，在预览中明确显示来源地区数、实际任务单元数和当前3类成果，不要求复制文件或逐地区重复上传。
11. **移动端必须提供可可靠选择 ZIP 的专用入口。** Android/Chromium 等环境不得只依赖无类型约束的通用文件选择器；管理员界面应保留“选择 ZIP 文件（手机推荐）”入口，使用非媒体 ZIP MIME/扩展名并移除 `capture`，避免系统误进入照片/视频选择器，同时不得删除原有通用文件、文件夹和参考资料上传能力。
12. **手机ZIP解压后必须等待北部权威索引加载完成并逐文件重新匹配。** 不得沿用ZIP刚解压时产生的空 `city/unit/district` 或陈旧 `autoMeta`；命中28份登记材料后必须显示“归档信息完整/可直接上传”，并使用共享多任务关系写入，不得显示“归档信息不完整”。
13. **管理员导入默认自动识别元数据。** 系统应优先依据已确认材料索引、文件名和目录名自动判断导入类型、成果类型、批次和任务关联；完全识别时不得要求管理员重复手动指定，人工下拉框仅作为无法识别或需要纠正时的兜底。
14. **河北省北部28份本批质控材料的权威索引必须作为识别基准。** 每份至少保存文件名、大小、SHA-256、唯一物理路径、第一轮批次、当前3类成果及其 `associationsByDataKey`；实际归档时应校验登记SHA-256。若原始文件尚未真正进入仓库，页面必须区分显示“已登记·待归档”，不得生成失效的可点击文件链接。
15. **所有上传成功提示必须包含：**“上传成功！稍等3~5分钟刷新网站即可查看新上传的文件。”
16. **上传整改答复不需要管理员密码。** 管理员密码只用于管理员导入、删除等管理操作。
17. **整改答复成功上传后，原位置立即显示“查看”和“替换”。** 页面重新打开或刷新后，也必须依据仓库中的答复文件恢复这两个按钮。
18. **“查看”必须打开当前批次最新答复文件；“替换”必须重新打开该位置的答复上传流程。**
19. **整改答复上传必须持续显示可变化的进度。** 至少区分文件读取、网络上传、GitHub确认和完成状态；不得长期停留在“准备上传”后直接跳到成功。
20. **网站初始化期间不得展示旧版基础页面或逐层变形。** 所有增强模块完成最终渲染后再一次性展示页面，并保留加载失败兜底。
21. **所有动态前端脚本必须使用当前语义化版本作为缓存键。** 启动流程应检查部署版本一致性，避免旧缓存与新资源混用。
22. **未经明确同意不得删除已有功能、提示、按钮、数据兼容逻辑或工作流。** 修复其他问题时必须先验证这些既有行为仍然有效。
23. **管理员混合/分块导入真实提交到 `main` 后必须显式触发 GitHub Pages 部署。** 不能只依赖由 `GITHUB_TOKEN` 产生的机器人提交再次触发 `push` 工作流；归档工作流必须在确认实际产生并推送提交后主动 `workflow_dispatch` 部署流程，确保 `data`、索引和仓库目录清单进入最新 Pages 产物。
24. **管理员动态导入的质控文件查看链接必须使用仓库原始文件直链或等价的立即可用地址。** 不得只依赖可能尚未刷新完成的 GitHub Pages 静态快照；文件已经进入 `main` 后，即使 Pages 仍在重新部署，用户点击“查看”也不得出现因旧发布产物导致的 404。
25. 每次程序代码或功能修改必须同步迭代 `VERSION` 和 `CHANGELOG.md`，并运行项目校验、管理员上传状态与在线凭证、管理员进度递归、移动端ZIP选择与重新匹配、北部3类权威共享质控索引/路由/统计、自动导入识别、导入后Pages显式部署与动态文件直链、答复工作流、实时进度和原子启动回归测试。
