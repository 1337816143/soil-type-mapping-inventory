# 项目维护约束

以下行为是项目所有者明确指定的现有需求。除非项目所有者在新的指令中明确要求变更，否则后续修改不得删除、弱化或替换这些行为。

1. **默认 GitHub Token 内置在前端代码中。** 不得擅自删除、置空、改为必须手动输入，或用其他凭证策略替换。
2. **所有上传成功提示必须包含：**“上传成功！稍等3~5分钟刷新网站即可查看新上传的文件。”
3. **上传整改答复不需要管理员密码。** 管理员密码只用于管理员导入、删除等管理操作。
4. **整改答复成功上传后，原位置立即显示“查看”和“替换”。** 页面重新打开或刷新后，也必须依据仓库中的答复文件恢复这两个按钮。
5. **“查看”必须打开当前批次最新答复文件；“替换”必须重新打开该位置的答复上传流程。**
6. **未经明确同意不得删除已有功能、提示、按钮、数据兼容逻辑或工作流。** 修复其他问题时必须先验证这些既有行为仍然有效。
7. 每次程序代码或功能修改必须同步迭代 `VERSION` 和 `CHANGELOG.md`，并运行项目校验和答复工作流回归测试。
