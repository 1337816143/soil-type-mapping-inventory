# 项目维护约束

以下行为是项目所有者明确指定的现有需求。除非项目所有者在新的指令中明确要求变更，否则后续修改不得删除、弱化或替换这些行为。

1. **默认 GitHub Token 内置在前端代码中。** 不得擅自删除、置空、改为必须手动输入，或用其他凭证策略替换。
2. **管理员参考资料和质控文件上传沿用原始 Git Data API `fetch + Bearer` 链路。** 未经真实 API 回归验证，不得通过全局替换 `window.fetch`、XHR 代理或其他传输层重写接管 Git Blob 请求。
3. **内置 Token 必须通过自动化在线验证。** 至少验证读取 `main` 引用和创建微型 Git Blob；验证过程不得输出 Token。浏览器中的临时覆盖不得删除或置空内置 Token。
4. **管理员上传进度必须在原位置更新。** 当前阶段和当前百分比直接替换原有内容，不得把上一条状态递归拼接为不断增长的日志；进度条必须同步变化。
5. **所有上传成功提示必须包含：**“上传成功！稍等3~5分钟刷新网站即可查看新上传的文件。”
6. **上传整改答复不需要管理员密码。** 管理员密码只用于管理员导入、删除等管理操作。
7. **整改答复成功上传后，原位置立即显示“查看”和“替换”。** 页面重新打开或刷新后，也必须依据仓库中的答复文件恢复这两个按钮。
8. **“查看”必须打开当前批次最新答复文件；“替换”必须重新打开该位置的答复上传流程。**
9. **整改答复上传必须持续显示可变化的进度。** 至少区分文件读取、网络上传、GitHub确认和完成状态；不得长期停留在“准备上传”后直接跳到成功。
10. **网站初始化期间不得展示旧版基础页面或逐层变形。** 所有增强模块完成最终渲染后再一次性展示页面，并保留加载失败兜底。
11. **所有动态前端脚本必须使用当前语义化版本作为缓存键。** 启动流程应检查部署版本一致性，避免旧缓存与新资源混用。
12. **未经明确同意不得删除已有功能、提示、按钮、数据兼容逻辑或工作流。** 修复其他问题时必须先验证这些既有行为仍然有效。
13. 每次程序代码或功能修改必须同步迭代 `VERSION` 和 `CHANGELOG.md`，并运行项目校验、管理员上传状态与在线凭证、答复工作流、实时进度和原子启动回归测试。
