name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    timeout-minutes: 30
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout triggering commit
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          sparse-checkout: |
            index.html
            upload-config.js
            page-enhancements.js
            page-enhancements-core.js
            reference-library.js
            admin-import.js
            pptx-auto-split.js
            assets
            data
            replies
          sparse-checkout-cone-mode: false

      - name: Validate deploy files
        shell: bash
        run: |
          set -euo pipefail
          test -f index.html
          test -f upload-config.js
          test -f page-enhancements.js
          test -f page-enhancements-core.js
          test -f reference-library.js
          test -f admin-import.js
          test -f pptx-auto-split.js
          test -f assets/soil-survey-logo.svg
          test -f data/admin-import-index.json
          grep -q 'calculateDashboardStats(dataKey)' index.html
          grep -q 'upload-config.js' index.html
          grep -q 'SOIL_UPLOAD_API_URL' upload-config.js
          grep -q 'page-enhancements.js' upload-config.js
          grep -q 'page-enhancements-core.js' page-enhancements.js
          grep -q 'reference-library.js' page-enhancements.js
          grep -q 'pptx-auto-split.js' page-enhancements.js
          grep -q './assets/soil-survey-logo.svg' page-enhancements-core.js
          grep -q 'missing-layout' page-enhancements-core.js
          grep -q '参考文件' reference-library.js
          grep -q 'admin-import.js' reference-library.js
          grep -q '管理员导入质控意见' admin-import.js
          grep -q '按原始幻灯片顺序连续拆分' pptx-auto-split.js
          if grep -q '<span>中国农业大学</span>' page-enhancements-core.js; then
            echo '检测到重复的中国农业大学文字，停止部署。'
            exit 1
          fi
          echo '部署文件校验通过。'

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
